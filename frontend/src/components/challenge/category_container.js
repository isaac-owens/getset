import {connect} from 'react-redux';
import Category from './category';

// mSTP = state =>(
//     {
//         category: state.cetegory
//     }
// );

// mDTP = dispatch =>(
//     {

//     }
// );

export default connect(null)(Category);